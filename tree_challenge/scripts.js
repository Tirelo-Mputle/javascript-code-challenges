let treeString = "";
// Set a loop that starts from 1 and runs as long as i is
// less than 5
for (let i = 1; i < 5; i++) {
  //with each iteration "*" is added to the tree string
  treeString += "*";
  //the resulting string is consoled each time.
  console.log(treeString);
}
